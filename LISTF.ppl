test("list for in", -> {
    let &lista |1,2,3|
    let &counter 0
    for &val in &lista
        assertEq(&lista.itemAt(&counter), &val)
        let &counter &counter + 1
    end
})

&idx := 0

test("list recorrer", -> {
    let &lista |1,2,3|
    assertEq(1, &lista.itemAt(&idx))
    recorrer&k 0, 2
        assertEq(&lista.itemAt(&k), &val)
    proximo
})