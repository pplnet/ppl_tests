*Ignore=V7

** TODO: Evitar usar dialogo, no hace falta...

test("repetir", -> {
    ACN(A:1,0)
    Repetir
      If Val(A:1) = 2
         Break
      EndIf
      ACN(A:1, Val(A:1)+1)
    Proximo
    assertEq(2, Val(A:1))
    
    try
       Repetir
         Cancelar
         Break
       Proximo
       ACN(Z:1,123)
    catch
      assertEq(NULL, Val(Z:1))
    end
})
