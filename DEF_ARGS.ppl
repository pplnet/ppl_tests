def concat(arg1, arg2)
  return arg1~arg2
end

def ppl_echo(msg)
  return msg
end

test("def func args", -> {
    assertEq('HolaMundo', concat('Hola', 'Mundo'))
    assertEq('Hello pipex!', ppl_echo('Hello pipex!'))
})
